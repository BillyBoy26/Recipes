<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.benjamin.recettes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application


        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RecipesActivity"
           >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".createForm.RecipeCreate"
            android:windowSoftInputMode="adjustPan"
            android:label="@string/create_a_recipe"
            />
        <activity android:name=".RecipeImport"
            android:label="@string/recipeImport"
            />
        <activity android:name=".category.CategoryList"
            android:label="@string/handle_category"
            />
        <activity android:name=".shoppingList.ShoppingList"
            android:label="@string/shopping_list"
            />
        <activity android:name=".recipeGroup.RecipeGroup"
            android:label="@string/application_name"
            />

    </application>

</manifest>